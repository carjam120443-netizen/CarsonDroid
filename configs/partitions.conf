# Experimental CarsonDroid disk/ISO layout
# Values are targets, not a replacement for the final dynamic partition metadata.
SYSTEM_SIZE_MB=4096
SYSTEM_EXT_SIZE_MB=1024
PRODUCT_SIZE_MB=2048
VENDOR_SIZE_MB=2048
CACHE_SIZE_MB=512
USERDATA_SIZE_MB=8192
BOOT_SIZE_MB=128
RECOVERY_SIZE_MB=128
EFI_SIZE_MB=64
